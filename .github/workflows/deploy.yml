name: Build and deploy VBC docs

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/deploy.yml"
      - "docs/**"
      - "mkdocs.yml"
      - "pyproject.toml"
      - "uv.lock"
      - "vbc/**"
      - "tests/test_docs_sync.py"
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    environment:
      name: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --extra docs --extra dev

      - name: Test docs sync
        run: uv run pytest tests/test_docs_sync.py -q

      - name: Build docs
        run: uv run mkdocs build

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/vbc_deploy
          chmod 600 ~/.ssh/vbc_deploy
          ssh-keyscan -H "${{ vars.VPS_HOST }}" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Deploy
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/vbc_deploy" \
            site/ \
            "${{ vars.VPS_USER }}@${{ vars.VPS_HOST }}:${{ vars.VPS_TARGET }}/"
